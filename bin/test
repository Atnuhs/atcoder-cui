#!/bin/bash
set -euo pipefail
trap 'echo "ERROR: line no = $LINENO, exit status = $?" >&2; exit 1' ERR

ROOT="$(cd $(dirname $0)/.. && pwd)"
source "${ROOT}/bin/.env"

PROBLEM=$1
FILE_SRC="solve/main.go"

oj t -c "go run ${FILE_SRC}" -d "${ROOT}/problems/${CONTESTNAME}/${PROBLEM}/tests"
